#!/mnt/56582F44582F226B/Andres/Documentos/Andres/1 Universidad/4to Semestre/Corte3/Bases de datos 1/Vet/bin/python

import logging
import optparse

import blueprint

parser = optparse.OptionParser('Usage: %prog [-q]')
parser.add_option('-q', '--quiet',
                  dest='quiet',
                  default=False,
                  action='store_true',
                  help='operate quietly')
options, args = parser.parse_args()

if options.quiet:
    logging.root.setLevel(logging.CRITICAL)

for name in blueprint.Blueprint.iter():
    print('  {0}'.format(name))
